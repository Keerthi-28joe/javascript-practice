// Combining shift() and unshift()
// Write a program to create an array of 3 items. Add a new item to the beginning using unshift(), then remove the first item using shift(). Print the updated array.

let objects=["books","vehicles","bangels"];
console.log(objects.unshift("rings"));
console.log(objects);
console.log(objects.shift());
console.log(objects);
