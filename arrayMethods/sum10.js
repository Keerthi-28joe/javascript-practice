// Combining push() and pop()
// Create an array of 3 countries. Add one more country using push(), then remove the last country using pop(). Print the updated array and the removed country
 
let countries=["India","China","America"];
console.log(countries.push("Africa"));
console.log(countries);
console.log(countries.pop());
console.log(countries);