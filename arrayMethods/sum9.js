// Split Method
// Write a program to split the string "dog,cat,bird" into an array and print the resulting array.

let word="dog,cat,bird";
let str=word.split(",");
console.log(str);

// split is used for change a string to array
// join is used for change array to string