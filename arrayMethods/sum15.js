// Push Multiple Elements
// Create an array of 2 colors. Use push() to add 2 more colors in one line and print the new array.

let colors=["red","pink"];
console.log(colors.push("orange","rose"));
console.log(colors);