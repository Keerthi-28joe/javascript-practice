// Split String with Different Delimiter
// Create a string "HTML-CSS-JavaScript". Use split() to break it into an array of 3 elements and print the array.

let sen=["HTML-CSS-JavaScript"];
let sep=(sen.split());
console.log(sep);